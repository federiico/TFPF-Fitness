import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TFPF-Fitness',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
